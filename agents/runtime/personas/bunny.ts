// bunny — the calendar agent. chipper, punctual.

import type { PersonaConfig } from "./index.js";

export const bunny: PersonaConfig = {
  species: "bunny",
  identity: "bunny",
  name: "bunny",
  watches: "calendar",
  systemPromptFile: "bunny.md",
};

export default bunny;
