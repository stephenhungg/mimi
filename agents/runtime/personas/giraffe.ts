// giraffe — the meeting-notes agent. thoughtful, observational.

import type { PersonaConfig } from "./index.js";

export const giraffe: PersonaConfig = {
  species: "giraffe",
  identity: "giraffe",
  name: "giraffe",
  watches: "notion",
  systemPromptFile: "giraffe.md",
};

export default giraffe;
