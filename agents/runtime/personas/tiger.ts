// tiger — the github agent. dry, technical, terse.

import type { PersonaConfig } from "./index.js";

export const tiger: PersonaConfig = {
  species: "tiger",
  identity: "tiger",
  name: "tiger",
  watches: "github",
  systemPromptFile: "tiger.md",
};

export default tiger;
