# scripts — dev tooling

One-off and recurring scripts. Run via `bun scripts/<name>.ts`.

Planned:
- `provision-notion-dbs.ts` — creates the 5 mimi. dbs in a notion workspace
- `seed-fixtures.ts` — seeds residents + events for demo
- `regenerate-asset.ts` — kicks higgsfield gen for a missing asset
- `deploy-all-workers.ts` — deploys all 4 workers in sequence
