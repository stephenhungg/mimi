# design/prompts — generation log

Every higgsfield/midjourney/gpt-image prompt that produced an asset.

When you generate something good, save the prompt here so we can regenerate variants or rebrand later.

Format: `<asset-name>.md` with the prompt body + the brand lock attached at gen time.
